# Development and testing dependencies for the MCP OAuth Gateway.
# Install with: pip install -r requirements-dev.txt

# First install the pinned runtime requirements.
-r requirements.txt

# Testing
pytest>=7.0.0

# starlette's TestClient imports the literal `httpx` package for its in-process
# ASGI transport. The gateway's runtime HTTP client is httpx2, so plain httpx is
# a test-only dependency and is intentionally kept out of requirements.txt.
httpx>=0.27.0
