# Core MCP Server Dependencies
# These are essential for the server to run
# For ML features (similarity search), also install: requirements-ml.txt

# MCP Core
mcp>=1.0.0
fastmcp>=0.1.0
pydantic>=2.0.0
networkx>=3.0
python-dateutil>=2.8.0

# Document processing (lightweight)
PyMuPDF>=1.23.0
python-docx>=1.0.0
python-multipart>=0.0.9
requests>=2.31.0

# Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0

# LLM Providers
anthropic>=0.18.0
openai>=1.0.0

# Core Server
python-dotenv>=1.0.0
fastapi>=0.100.0
uvicorn>=0.20.0

# Note: For similarity search features, install requirements-ml.txt separately
# This keeps the base installation smaller and faster
