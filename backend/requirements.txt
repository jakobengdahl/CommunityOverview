# Core MCP Server Dependencies
# Optimized for GitHub Codespaces (minimal disk usage)
# For development/testing: pip install -r requirements-dev.txt

# MCP Core
mcp>=1.0.0
fastmcp>=0.1.0
pydantic>=2.0.0
networkx>=3.0
python-dateutil>=2.8.0

# Document processing
pypdf>=4.0.0
python-docx>=1.0.0
python-multipart>=0.0.9
requests>=2.31.0

# LLM Providers
anthropic>=0.18.0
openai>=1.0.0

# Core Server
python-dotenv>=1.0.0
fastapi>=0.100.0
uvicorn>=0.20.0

# String similarity (lightweight)
python-Levenshtein>=0.21.0

# ===== ML Dependencies (OPTIMIZED FOR DISK SPACE) =====
# Install PyTorch CPU-only version (much smaller than CUDA version)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# numpy - used directly by vector_store.py for embedding index
numpy>=1.24.0

# Sentence transformers for semantic similarity
# This will use the CPU-only PyTorch installed above
sentence-transformers>=2.2.0

# Lightweight ML utilities
scikit-learn>=1.0.0
