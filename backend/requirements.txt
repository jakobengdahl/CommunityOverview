# Core MCP Server Dependencies
# Optimized for GitHub Codespaces (minimal disk usage)
# For development/testing: pip install -r requirements-dev.txt

# MCP Core
mcp>=1.0.0
fastmcp>=0.1.0
pydantic>=2.0.0
networkx>=3.0
python-dateutil>=2.8.0

# Skills loader
httpx2>=2.0.0
pyyaml>=6.0

# Document processing
pypdf>=4.0.0
python-docx>=1.0.0
python-multipart>=0.0.9
requests>=2.31.0

# LLM Providers
anthropic>=0.18.0
openai>=1.0.0

# Core Server
python-dotenv>=1.0.0
fastapi>=0.100.0
uvicorn>=0.20.0

# String similarity (MIT-licensed, C++ optimized)
rapidfuzz>=3.0.0

# numpy - used directly by vector_store.py for the embedding index and
# cosine-similarity search. Pure-PyPI and lightweight, so it stays in the base
# requirements: semantic *search* over existing embeddings works without the ML
# extras. Generating new embeddings needs the ML stack (requirements-ml.txt).
numpy>=1.24.0
