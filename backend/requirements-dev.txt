# Development and Testing Dependencies
# Install with: pip install --no-cache-dir -r requirements-dev.txt

# First install core requirements
-r requirements.txt

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Linting and formatting (replaces black; config in root pyproject.toml).
# Pinned to the 0.15 line so `ruff format` output matches the CI pin
# (ruff==0.15.8 in .github/workflows/ci.yml) — formatter defaults can shift
# between minor releases, which would make the non-required python-lint job
# disagree with a locally-clean tree.
ruff>=0.15.8,<0.16
